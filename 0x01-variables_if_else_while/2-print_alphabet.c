/**
 * main - main block
 * Description: Get a random number and check it's last digit, compare it with 5
 * Return: 0
 */
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	char c = 'a';

	while (c <= 'z'){
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
